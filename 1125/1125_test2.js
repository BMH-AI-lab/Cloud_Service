const arrays = [3, 7, 2, 9, 5, 1, 8]

// 1. 5보다 큰 숫자만 필터링 
const evens = arrays.filter((arrays) => arrays > 5);
console.log(evens);

// 2. 각 숫자를 제곱 
const result = arrays.map((arrays) => arrays ** 2);
console.log(result);

// 3. 오름차순 정렬
const sort1 = arrays.sort();
console.log(sort1);